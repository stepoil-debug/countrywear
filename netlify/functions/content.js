const {response,requireAuth,readFile,writeText}=require('./_shared');
const files={catalog:'data/catalog.json',settings:'data/settings.json'};
exports.handler=async(event)=>{if(event.httpMethod==='OPTIONS')return response(204,{});if(!requireAuth(event))return response(401,{error:'Sessão inválida ou expirada'});const key=(event.queryStringParameters||{}).file,path=files[key];if(!path)return response(400,{error:'Arquivo inválido'});try{if(event.httpMethod==='GET'){const f=await readFile(path);return response(200,{data:JSON.parse(f.content)})}if(event.httpMethod==='PUT'){const body=JSON.parse(event.body||'{}'),data=body.data;if(!data||typeof data!=='object')return response(400,{error:'Dados inválidos'});const pretty=JSON.stringify(data,null,2)+'\n';await writeText(path,pretty,`admin: atualizar ${key}`);return response(200,{ok:true})}return response(405,{error:'Método não permitido'})}catch(e){return response(500,{error:e.message})}};
